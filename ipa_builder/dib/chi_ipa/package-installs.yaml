# needed to have enough network drivers on debian
linux-image-generic:

# on debian, bnx2x drivers missing even with linux-image-generic
firmware-bnx2x:
firmware-qlogic: 
firmware-misc-nonfree:

# needed for dynamic-login on debian
passwd:
